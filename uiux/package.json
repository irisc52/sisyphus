{
  "name": "sisyphus-ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:extension": "vite build && rm -rf ../extension/popup-ui && cp -r dist ../extension/popup-ui",
    "build:ios": "vite build && mkdir -p '../extension/Sisyphus/Shared (App)/Resources/Dashboard' && cp dist/dashboard.html '../extension/Sisyphus/Shared (App)/Resources/Dashboard/' && cp -r dist/assets '../extension/Sisyphus/Shared (App)/Resources/Dashboard/'",
    "preview": "vite preview"
  },
  "dependencies": {
    "lucide-react": "^0.563.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^7.3.1"
  }
}
